spring:
  datasource:
    url: jdbc:h2:mem:skatestdb
    driverClassName: org.h2.Driver
    username: root
    password: pass
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

metric:
  interceptor: disabled
